---
layout: base.njk
title: Golden Wings Documentary Screening - October 26, 2025
description: Join us for the premiere screening of Golden Wings, featuring Robyn Stewart's incredible 50+ year career as an American Airlines flight attendant
---

{% set event = siteConfig.event %}
{% set theme = siteConfig.theme %}
{% set video = siteConfig.video %}

<main class="min-h-screen bg-gradient-to-b from-blue-50/60 to-white">
  <!-- Hero Section with Video Background -->
  <header class="hero relative overflow-hidden text-white min-h-screen flex items-center">
    <div class="hero__media">
      <video
        class="{% if video.hideOnMobile %}hidden md:block{% endif %}"
        autoplay
        loop
        muted
        playsinline
        poster="/videos/poster.jpg"
      >
        <source src="/videos/clouds-aerial.mp4" type="video/mp4">
        <source src="/videos/clouds-aerial.webm" type="video/webm">
      </video>
    </div>

    <div class="hero__overlay"></div>

    <div class="hero__content text-center md:text-left">
      <div class="badge">
        <span class="badge__icon">✈️</span>
        {{ theme.tagline }}
      </div>
      <h1 class="hero__title animate-fade-in">
        {{ event.title }}
      </h1>
      <p class="hero__tagline">{{ theme.missionStatement }}</p>
      <p class="hero__subtitle">
        Screening {{ event.date }} · {{ event.timePST }} PST / {{ event.timeCST }} CST / {{ event.timeEST }} EST
      </p>
      <a href="#rsvp" class="hero__cta">
        RSVP Now
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </header>

  <!-- Thematic Highlights -->
  <section class="theme-highlight">
    <div class="max-w-6xl mx-auto">
      <h2 class="section-title">Skyward Courage</h2>
      <p class="section-subtitle">
        Inspired by Robyn Stewart's fearless service, we celebrate the courage that carried passengers safely across the globe for over five decades.
      </p>
      <div class="theme-highlight__grid">
        {% for value in theme.values %}
        <article class="value-card">
          <h3 class="value-card__title">{{ value.title }}</h3>
          <p class="value-card__description">{{ value.description }}</p>
        </article>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="about-section">
    <div class="max-w-5xl mx-auto">
      <h2 class="section-title">About the Film</h2>
      <p>
        <strong>{{ event.title }}</strong> chronicles the remarkable career of Robyn Stewart, an American Airlines flight attendant whose {{ event.yearsInService }} in the skies witnessed the transformation of commercial aviation.
      </p>
      <p>
        Through intimate interviews and archival footage, this documentary captures stories of courage, dedication, and the evolution of an industry that connected the world.
      </p>
      <blockquote>
        "Each flight carried a story of courage and compassion. <strong>Golden Wings</strong> gathers them into one inspiring journey above the clouds."
      </blockquote>
      <div class="timeline">
        <div class="timeline__item">
          <strong>Premiere Boarding</strong>
          Doors open 20 minutes before showtime with sky lounge refreshments inspired by Robyn's first transatlantic route.
        </div>
        <div class="timeline__item">
          <strong>Feature Presentation</strong>
          A {{ event.duration }} cinematic tribute to unwavering service, filmed across historic American Airlines destinations.
        </div>
        <div class="timeline__item">
          <strong>Live Q&amp;A Touchdown</strong>
          Stay after the credits for a live conversation with director Caleb Mills Stewart.
        </div>
      </div>
    </div>
  </section>

  <!-- RSVP Section -->
  <section id="rsvp" class="bg-blue-50/60 py-16">
    <div class="max-w-5xl mx-auto px-6">
      <div class="rsvp-card">
        <h2>Reserve Your Seat</h2>

        <form id="rsvp-form" class="space-y-6">
          <div class="form-field">
            <label for="name">Full Name *</label>
            <input
              type="text"
              id="name"
              name="name"
              required
              autocomplete="name"
            >
          </div>

          <div class="form-field">
            <label for="email">Email Address *</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              autocomplete="email"
            >
          </div>

          <div class="form-field">
            <label for="phone">Phone Number</label>
            <input
              type="tel"
              id="phone"
              name="phone"
              inputmode="tel"
              autocomplete="tel"
            >
          </div>

          <div class="form-field">
            <label for="specialRequests">Special Requests</label>
            <textarea
              id="specialRequests"
              name="specialRequests"
              rows="3"
              placeholder="Dietary restrictions, accessibility needs, etc."
            ></textarea>
          </div>

          <button type="submit" class="primary-button">
            Confirm RSVP
          </button>
        </form>

        <p class="note text-center mt-6">
          You'll receive a confirmation email with event details, a calendar invite, and priority boarding instructions.
        </p>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="max-w-4xl mx-auto">
      <p class="text-lg mb-2">Golden Wings Documentary</p>
      <p class="note">Directed by Caleb Mills Stewart</p>
      <p class="note mt-4">
        <a href="{{ siteConfig.contact.website }}" class="footer__link">{{ siteConfig.contact.website | replace('https://', '') }}</a>
      </p>
      <p class="note mt-2">
        Contact the flight crew: <a href="mailto:{{ siteConfig.contact.email }}" class="footer__link">{{ siteConfig.contact.email }}</a>
      </p>
    </div>
  </footer>
</main>
